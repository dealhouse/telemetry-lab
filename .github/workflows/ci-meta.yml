name: CI (meta)

on:
  pull_request:
  push:
    branches: [main]

jobs:
  meta:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Meta checks
        run: scripts/ci/check-meta.sh
